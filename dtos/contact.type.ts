export type contactData = {
    name: string,
    email: string,
    phone: string | undefined,
};

export type contacts = {
    id: number,
    name: string,
    email: string,
    phone: string | null,
};